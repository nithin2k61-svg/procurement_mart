version: 2
sources:

  - name: ppswh_payment_sources
    description: List of all PPS source tables
    database:  |
      {%- if  target.name == "dev" -%} DEV_PAYMENTS_WAREHOUSE
      {%- elif target.name == "qa"  -%} QA_PAYMENTS_WAREHOUSE
      {%- elif target.name == "uat"  -%} PROD_PAYMENTS_WAREHOUSE
      {%- elif target.name == "prod"  -%}  PROD_PAYMENTS_WAREHOUSE
      {%- else -%} invalid_database
      {%- endif -%} 
    schema: payments_mart
    tables:
      - name: dimprovider
        description: Table has the  data for Payments
