version: 2
sources:
  - name: sequence_sources
    description: List of all ZeDW Datamart source tables
    database: |
      {%- if  target.name == "dev" -%} dev_finance_warehouse
      {%- elif target.name == "qa"  -%} qa_finance_warehouse
      {%- elif target.name == "uat"  -%} uat_finance_warehouse
      {%- elif target.name == "prod"  -%} prod_finance_warehouse
      {%- else -%} invalid_database
      {%- endif -%}
    schema: SOURCE
    tables:
      - name: dimbusinessunitidkey
        description: dimbusinessunitid sequence generation
      - name: dimsourcesystemidkey
        description: dimsourcesystemid sequence generation    
      - name: dimproducttypeidkey
        description: dimproducttypeid sequence generation
      - name: dimclientidkey
        description: dimclientidkey sequence generation
      - name: dimprovideridkey
        description: dimprovideridkey sequence generation
      - name: dimsupplieridkey
        description: dimsupplieridkey sequence generation
      - name: dimcompanyidkey
        description: dimcompanyidkey sequence generation
      - name: dimspendcategoryidkey
        description: dimspendcategoryidkey sequence generation
      - name: dimcostcenteridkey
        description: dimcostcenteridkey sequence generation
      - name: dimcurrencyidkey
        description: dimcurrencyidkey sequence generation
      - name: dimbuyeridkey
        description: dimbuyeridkey sequence generation
      - name: dimlocationidkey
        description: dimlocationidkey sequence generation
      - name: dimprojectidkey
        description: dimprojectidkey sequence generation
        
  - name: metrics_sequences
    description: List of all ZeDW Datamart source tables
    database: |
      {%- if  target.name == "dev" -%} dev_finance_warehouse
      {%- elif target.name == "qa"  -%} QA_finance_warehouse
      {%- elif target.name == "uat"  -%} UAT_finance_warehouse
      {%- elif target.name == "prod"  -%} PROD_finance_warehouse
      {%- else -%} invalid_database
      {%- endif -%}
    schema: SOURCE
    tables:
      - name: METRIC_ID
        description: METRIC_ID sequence generation
      - name: METRIC_CATEGORY_ID
        description: METRIC_CATEGORY_ID sequence generation
      - name: METRIC_USER_ID
        description: METRIC_USER_ID sequence generation
      - name: METRIC_KPI_ID
        description: METRIC_KPI_ID sequence generation

  - name: reference_sequences
    description: List of all reference_sources sequence
    database: |
      {%- if  target.name == "dev" -%} dev_finance_warehouse
      {%- elif target.name == "qa"  -%} qa_finance_warehouse
      {%- elif target.name == "uat"  -%} uat_finance_warehouse
      {%- elif target.name == "prod"  -%} prod_finance_warehouse
      {%- else -%} invalid_database
      {%- endif -%}
    schema: REFERENCE
    tables:
      - name: dimaccountskey
        description: dimaccountskey sequence generation